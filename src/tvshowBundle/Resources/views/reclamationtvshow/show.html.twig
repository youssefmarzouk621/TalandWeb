{% extends 'base.html.twig' %}

{% block body %}
    <h1>Reclamationtvshow</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamationtvshow.id }}</td>
            </tr>
            <tr>
                <th>Reason</th>
                <td>{{ reclamationtvshow.reason }}</td>
            </tr>
            <tr>
                <th>Responsetype</th>
                <td>{{ reclamationtvshow.responsetype }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ reclamationtvshow.etat }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('reclamationtvshow_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('reclamationtvshow_edit', { 'id': reclamationtvshow.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
