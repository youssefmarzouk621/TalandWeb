{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commentairetvshow</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentairetvshow.id }}</td>
            </tr>
            <tr>
                <th>Idu</th>
                <td>{{ commentairetvshow.Userid.username }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ commentairetvshow.content }}</td>
            </tr>
            <tr>
                <th>Idtvshow</th>
                <td>{{ commentairetvshow.idtvshow }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commentairetvshow_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('commentairetvshow_edit', { 'id': commentairetvshow.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
